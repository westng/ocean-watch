interface:
  display_name: "巨量千川计划助手"
  short_description: "千川授权、模板、计划、素材及全域与乘方多维效果报表"
  default_prompt: "Use $qc-plan-monitor to understand my Qianchuan intent, use Ocean Watch MCP for local template reads, route remaining operations through the bundled Go CLI, and preview writes safely."
dependencies:
  tools:
    - type: "mcp"
      value: "ocean-watch"
      description: "Ocean Watch local read-only template tools"
      transport: "stdio"
